import React from 'react';
import { Panel } from 'react-bootstrap';

const RFSPost = () => 
    <Panel>
        <h3>Initial Post</h3>
        <p />
        <span>This is the initial post.  It's totally static at this point -- in fact, it's hardcoded into the JS file.</span>
    </Panel>

export default RFSPost;